#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# this loads all dependencies for us as application starts. Word docs, CVS, etc. should be loaded here. Don't worry about this now.
# Do any other automated setup that you need to do here
